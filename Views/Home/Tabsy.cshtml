<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>Tabsy Userguide</title>
        <link rel="icon" href="@Url.Action("Serve", "Home", new { id = "personal_logo" })" type="image/png" sizes="64x64" />
        <script src="https://cdn.tailwindcss.com"></script>
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
        <style>
            @@import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap');

            body {
                font-family: 'Inter', sans-serif;
                background-color: #ffffff;
                background-image: radial-gradient(#cbd5e1 1.5px, transparent 1.5px), linear-gradient(to right, #f1f5f9 1px, transparent 1px), linear-gradient(to bottom, #f1f5f9 1px, transparent 1px);
                background-size: 24px 24px, 96px 96px, 96px 96px;
                background-attachment: fixed;
            }

            #pillow-container {
                position: fixed;
                top: 0;
                left: 0;
                width: 100%;
                height: 100%;
                overflow: hidden;
                pointer-events: none;
                z-index: 0;
            }

            .pillow {
                position: absolute;
                background-color: #e2e8f0;
                border-radius: 30%;
                opacity: 0.6;
                box-shadow: inset -2px -2px 6px rgba(0,0,0,0.05);
                animation: fall linear forwards;
                pointer-events: none;
            }

            @@keyframes fall {
                0% {
                    top: -10%;
                    opacity: 0;
                    transform: rotate(0deg);
                }

                10% {
                    opacity: 0.8;
                }

                90% {
                    opacity: 0.8;
                }

                100% {
                    top: 110%;
                    opacity: 0;
                    transform: rotate(360deg);
                }
            }

            .relative-content {
                position: relative;
                z-index: 10;
            }
        </style>
    </head>
    <body class="text-slate-800 antialiased flex flex-col min-h-screen">
        <div id="pillow-container"></div>

        <nav class="relative-content px-6 py-4 flex items-center justify-between max-w-7xl mx-auto w-full">
            <a href="@Url.Action("Index", "Home")" class="flex items-center space-x-2 text-slate-900 hover:text-slate-600 transition font-medium">
                <i class="fa-solid fa-arrow-left"></i>
                <span>Back to Home</span>
            </a>
            <div class="flex items-center space-x-2">
                <img src="@Url.Action("Serve", "Home", new { id = "tabsy_icon" })" alt="Tabsy Icon" class="w-8 h-8 rounded-lg shadow-sm">
                <span class="font-bold text-lg text-slate-900">Tabsy</span>
            </div>
        </nav>

        <main class="relative-content container mx-auto px-4 py-8 flex-grow">
            <h1 class="text-3xl md:text-4xl font-bold mb-10 text-center text-slate-900">How To Use Tabsy</h1>
            
            <div class="flex flex-col md:flex-row gap-10 items-start justify-center max-w-6xl mx-auto">
                <!-- Video Section -->
                <div class="w-full md:w-[350px] flex-shrink-0 flex justify-center">
                    <div class="relative rounded-[2rem] overflow-hidden shadow-2xl border-4 border-slate-900 bg-black w-[320px] h-[640px]">
                        <iframe 
                            class="w-full h-full" 
                            src="https://www.youtube.com/embed/7lc2rhjwZz0" 
                            title="Tabsy Demo" 
                            frameborder="0" 
                            allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" 
                            allowfullscreen>
                        </iframe>
                    </div>
                </div>

                <!-- Text Section -->
                <div class="flex-1 bg-white/90 backdrop-blur-md p-8 md:p-10 rounded-2xl shadow-lg border border-slate-200">
                    <h2 class="text-2xl font-bold mb-6 text-slate-900 border-b border-slate-200 pb-2">User Guide</h2>
                    <div class="prose prose-slate max-w-none text-slate-600 leading-relaxed">
                       <p class="text-lg mb-4">Welcome to Tabsy! This guide will help you get started with tracking shared expenses and debts effortlessly.</p>
                       
                       <div class="space-y-6">
                            <div>
                                <h3 class="font-bold text-slate-800 text-lg mb-2">1. Installation</h3>
                                <p>Download Tabsy from the <a href="https://apps.apple.com/us/app/tabsy/id6755607962" class="text-blue-600 hover:underline">App Store</a> or <a href="https://play.google.com/store/apps/details?id=com.tabtracker.app" class="text-blue-600 hover:underline">Google Play</a>. Once installed, open the app to begin.</p>
                            </div>

                            <div>
                                <h3 class="font-bold text-slate-800 text-lg mb-2">2. Creating a Tab</h3>
                                <p>Open the app and click the plus button, in the input box type the name of the person you are opening a tab with.</p>
                            </div>

                            <div>
                                <h3 class="font-bold text-slate-800 text-lg mb-2">3. logging transactions</h3>
                                <p>Click the tab you just made to start logging lends, borrows and payments. To log transactions press the plus button in the tab details screen and enter the required inputs then press save.</p>
                            </div>

                            <div>
                                <h3 class="font-bold text-slate-800 text-lg mb-2">4. Requesting payments or sending updates</h3>
                                <p>When in a tab, press the Send Update or Request Payment that appears on the top right of the Summary card. This will create a summary of all the lends or borrows all the way up to the point where the tab was last settled. If you need to share the entire history you may do so by going to the all time tab and scroll all the way down until you see the "Click to send history" clicking this button will send all the transactions</p>
                            </div>

                            <div>
                                <h3 class="font-bold text-slate-800 text-lg mb-2">How to delete tabs or transactions</h3>
                            <p>To delete a tab hold down the tab or transaction until you see the options menu come up, press then delete button. For a tab the delete button will begin the deletion process which allows the user a 30 grace period to recover the deleted tab. If a tab was deleted by accident it can be recovered by pressing hamburger menu > settings > deleted tabs. After 30 of a tab being deleted it will be permanently deleted</p>
                            </div>
                            <!-- Add more steps here as needed -->
                       </div>
                    </div>
                </div>
            </div>
        </main>

        <footer class="relative-content text-center py-8 text-slate-500 text-sm">
            &copy; @DateTime.Now.Year Trafficy. All rights reserved.
        </footer>

        <script>
            document.addEventListener('DOMContentLoaded', () => {
                const container = document.getElementById('pillow-container');
                if (!container) return;

                function createPillow() {
                    const pillow = document.createElement('div');
                    pillow.classList.add('pillow');
                    
                    const size = Math.random() * 30 + 20; 
                    const left = Math.random() * 100; 
                    const duration = Math.random() * 5 + 8; 
                    const color = Math.random() > 0.5 ? '#cbd5e1' : '#e2e8f0'; 

                    pillow.style.width = `${size}px`;
                    pillow.style.height = `${size * 0.8}px`; 
                    pillow.style.left = `${left}%`;
                    pillow.style.backgroundColor = color;
                    pillow.style.animationDuration = `${duration}s`;

                    container.appendChild(pillow);

                    setTimeout(() => {
                        pillow.remove();
                    }, duration * 1000);
                }

                setInterval(createPillow, 600); 
            });
        </script>
    </body>
</html>